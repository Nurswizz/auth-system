const emailService = {
    sendVerificationCode: async (email, token) => {
        // Logic to send verification code via email
        console.log(`Sending verification code to ${email} with token: ${token}`);
    }
}